<form method="post" action="{{ path('app_financeur_delete', {'idFinanceur': financeur.idFinanceur}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ financeur.idFinanceur) }}">
    <button class="btn">Delete</button>
</form>
