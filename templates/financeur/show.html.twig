{% extends 'base.html.twig' %}

{% block title %}Financeur{% endblock %}

{% block body %}
    <h1>Financeur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdFinanceur</th>
                <td>{{ financeur.idFinanceur }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ financeur.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_financeur_index') }}">back to list</a>

    <a href="{{ path('app_financeur_edit', {'idFinanceur': financeur.idFinanceur}) }}">edit</a>

    {{ include('financeur/_delete_form.html.twig') }}
{% endblock %}
