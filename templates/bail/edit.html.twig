{% extends 'base.html.twig' %}

{% block title %}Edit Bail{% endblock %}

{% block body %}
    <h1>Edit Bail</h1>

    {{ include('bail/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_bail_index') }}">back to list</a>

    {{ include('bail/_delete_form.html.twig') }}
{% endblock %}
