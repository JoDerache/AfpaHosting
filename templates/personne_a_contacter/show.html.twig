{% extends 'base.html.twig' %}

{% block title %}PersonneAContacter{% endblock %}

{% block body %}
    <h1>PersonneAContacter</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdPersonneContact</th>
                <td>{{ personne_a_contacter.idPersonneContact }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ personne_a_contacter.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ personne_a_contacter.prenom }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ personne_a_contacter.telephone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_personne_a_contacter_index') }}">back to list</a>

    <a href="{{ path('app_personne_a_contacter_edit', {'idPersonneContact': personne_a_contacter.idPersonneContact}) }}">edit</a>

    {{ include('personne_a_contacter/_delete_form.html.twig') }}
{% endblock %}
