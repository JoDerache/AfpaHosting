<form method="post" action="{{ path('app_personne_a_contacter_delete', {'idPersonneContact': personne_a_contacter.idPersonneContact}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ personne_a_contacter.idPersonneContact) }}">
    <button class="btn">Delete</button>
</form>
