{% extends 'base.html.twig' %}

{% block title %}Avertissement index{% endblock %}

{% block body %}
    <h1>Avertissement index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>IdAvertissement</th>
                <th>DateAvertissement</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for avertissement in avertissements %}
            <tr>
                <td>{{ avertissement.idAvertissement }}</td>
                <td>{{ avertissement.dateAvertissement ? avertissement.dateAvertissement|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('app_avertissement_show', {'idAvertissement': avertissement.idAvertissement}) }}">show</a>
                    <a href="{{ path('app_avertissement_edit', {'idAvertissement': avertissement.idAvertissement}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_avertissement_new') }}">Create new</a>
{% endblock %}
